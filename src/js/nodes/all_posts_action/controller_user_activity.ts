import {gEBI, dCE} from "../../units/compress_f.js";
import {generate_random_neuron_image} from "./load_random_neurons";

document.addEventListener('DOMContentLoaded',function(){
    if(gEBI("loading_flag") != false){
        generate_random_neuron_image();
    }
});
//content volume
    //get posts
        //get size
        //if (size more than ...)
        //turn off animation


//search controller
    //export array search elements or rows with search element to save more information
    //divide main search function and addition async
    //clusterization by tags

//combinations keypress events

//post save

// async timer to update graph information
/*
    Tags:
    Effectiveness_graph: max_connections / connection
    Connections:
*/ 
